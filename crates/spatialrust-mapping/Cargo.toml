[package]
name = "spatialrust-mapping"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Trajectories, pose graphs, and localization primitives for SpatialRust"

[features]
default = []
vision-odometry = ["dep:spatialrust-vision"]
scan-icp = ["dep:spatialrust-registration"]

[dependencies]
spatialrust-core.workspace = true
spatialrust-math.workspace = true
spatialrust-sync.workspace = true
spatialrust-vision = { workspace = true, optional = true, features = ["odometry"] }
spatialrust-registration = { workspace = true, optional = true, features = ["register-icp"] }
thiserror.workspace = true

[dev-dependencies]
spatialrust-records.workspace = true
