[package]
name = "spatialrust-viz"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Backend-independent visualization contracts for SpatialRust"

[features]
default = []
core = ["dep:spatialrust-core"]
serde = ["dep:serde", "spatialrust-math/serde"]

[dependencies]
serde = { workspace = true, optional = true }
spatialrust-core = { workspace = true, optional = true }
spatialrust-math.workspace = true
thiserror.workspace = true
