[package]
name = "spatialrust-semantic"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Semantic entities, embeddings, and multimodal fusion/search for SpatialRust"

[features]
default = []
model = ["dep:spatialrust-ai", "dep:spatialrust-tensor"]

[dependencies]
spatialrust-core.workspace = true
spatialrust-math.workspace = true
spatialrust-records.workspace = true
spatialrust-ai = { workspace = true, optional = true }
spatialrust-tensor = { workspace = true, optional = true }
thiserror.workspace = true
