# Visual Epic 135 receipt

Date: 2026-07-28

## Scope

- Added `spatialrust-viewer` with a dependency-light default build.
- Added a versioned portable state containing camera, viewport, ordered layer
  presentation, inspector selection, and validated drag/drop queue.
- Added a deterministic orbit/pan/zoom/focus input reducer.
- Added an opt-in winit native window shell behind `native`.
- Added normal, voxel, plane, cluster, correspondence, bounds, and
  search-radius debug overlays.

## Contract evidence

- Scene synchronization retains user visibility/style edits by `LayerId`.
- Attribute summaries preserve point/scalar counts and active attribute names.
- Unsupported dropped file types and missing layer IDs fail closed.
- Every overlay owns its storage and exposes a borrowed `VisualLayer`; it does
  not upload or read back GPU data.
- IDs use `debug/{namespace}/{kind}` and remain stable across reconstruction.
- Canonical fixtures assert exact segment/point counts for all overlay kinds.
- Native dependencies are absent from `spatialrust-viewer` default features.

## Validation

- `cargo test -p spatialrust-viewer`
- `cargo test -p spatialrust-viewer --all-features`
- `cargo clippy -p spatialrust-viewer --all-features -- -D warnings`
- `cargo check -p spatialrust --features viewer`
- `cargo check -p spatialrust --features viewer-native --example native_viewer`
