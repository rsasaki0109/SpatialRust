# Visual Epic 140 receipt

Date: 2026-07-28

## Scope

- Added a fail-closed typed Visual release gate and Visual API stability
  registry.
- Added a fail-closed whole-image renderer conformance example with exact
  upload, frame, and readback ledgers.
- Added Linux/Windows/macOS Visual conformance and Web adapter CI gates.
- Added the user guide, `visual-1` migration policy, and committed aggregate
  release receipt.

## Denial coverage

- Missing, skipped, or duplicate conformance cases.
- Missing, duplicate, future-dated, or more-than-30-day-old receipts.
- Image mismatch, hidden readback, transfer, LOD residency/concurrency, browser
  request, Python copy, and state round-trip budget overruns.
- Missing/duplicate examples, incomplete security evidence, experimental APIs,
  and wrong migration policy.

## Validation

- `cargo test -p spatialrust-platform visual`
- `cargo run -p spatialrust --no-default-features --features render-wgpu --example visual_headless_conformance`
- `cargo run -p spatialrust --no-default-features --features platform --example visual_release_gate`
- Visual Linux/Windows/macOS CI conformance matrix
- wasm32/WebGPU build and real headless-browser smoke
- Python 3.8/current wheel, Jupyter tests, and executable notebook smoke
