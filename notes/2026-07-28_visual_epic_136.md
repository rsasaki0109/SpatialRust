# Visual Epic 136 receipt

Date: 2026-07-28

## Scope

- Added individually gated scene, Gaussian, mapping, camera, and semantic
  adapters to `spatialrust-viewer`.
- Added direct borrowed triangle mesh layers and owned surfel, Gaussian,
  trajectory, pose-graph, camera-frustum, and semantic visual geometry.
- Added a synchronized borrowed RGB/depth/cloud timeline and calibrated pixel
  inspector.

## Contract evidence

- Direct mesh adaptation preserves the source position pointer and allocates
  zero generated bytes.
- Generated adapters record source identity/count, output count, and exact
  generated bytes.
- Gaussian RGB columns and opacity, surfel radius, semantic confidence, and
  trajectory/graph segment parity are asserted by canonical fixtures.
- RGB-D frames require exact image lengths, matching cloud payload/timestamp
  presence, non-decreasing timeline order, and configured maximum sensor skew.
- Invalid depth remains visible as a sample but does not fabricate a 3D point.
- All external adapter families are absent from default viewer features.

## Validation

- `cargo test -p spatialrust-viewer --features adapters-all`
- `cargo clippy -p spatialrust-viewer --features adapters-all -- -D warnings`
- `cargo check -p spatialrust --features viewer-full`
